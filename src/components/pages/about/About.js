import React from 'react';
import styles from "./About.module.css"

const About = () => {
    return (
        <div>
            <h1 className={styles.title}>Страница о нас</h1>
        </div>
    );
};

export default About;